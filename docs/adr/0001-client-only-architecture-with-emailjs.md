# 純前端架構，以 EmailJS 背景寄信取代後端/資料庫

告白卡片是寄給單一特定受邀人的一次性客製化頁面，不需要多人使用、不需要帳號系統、也不需要保存歷史回應紀錄。因此選擇不建立後端伺服器或資料庫，改用 EmailJS 在受邀人選定日期送出的當下，直接從瀏覽器端呼叫寄信服務，將「已答應＋選定日期」以 email 通知發送人。這個決定放棄了回應紀錄的可查詢性與寄送失敗時的重試能力，換取零維運成本與最簡單的靜態網站部署方式。
